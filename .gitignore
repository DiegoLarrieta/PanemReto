
# Intermediate pipeline files — regenerated by running 00_data_pipeline.ipynb
# Keep only datanomodifier.csv in repo — the two larger files are regeneratable
data/intermediate/complete data with weather.csv
data/intermediate/df_complete_186_files.csv
